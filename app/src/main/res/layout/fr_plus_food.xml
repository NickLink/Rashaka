<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@color/background_white">

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_marginTop="@dimen/inner_padding"
        android:layout_centerHorizontal="true"
        app:srcCompat="@drawable/icon_plus_log_food"/>

    <TextView
        android:id="@+id/title"
        style="@style/InfoLightBlackTextBold"
        android:textSize="@dimen/text_size_giant"
        android:layout_marginTop="@dimen/inner_padding_small"
        android:layout_marginBottom="@dimen/inner_padding_small"
        android:text="Log Food"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/imageView"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_below="@+id/title">

        <include layout="@layout/item_log_food"
                 android:id="@+id/drink_alarm_item1"/>

        <include layout="@layout/item_log_food"
                 android:id="@+id/drink_alarm_item2"/>

        <include layout="@layout/item_log_food"
                 android:id="@+id/drink_alarm_item3"/>

        <include layout="@layout/item_log_food"
                 android:id="@+id/drink_alarm_item4"/>

    </LinearLayout>


    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/main_breeze"
        android:layout_alignParentBottom="true"
        android:padding="@dimen/inner_padding">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Add New Alarm"/>
    </FrameLayout>

</RelativeLayout>